k8s:
  dns: kube-dns.kube-system
  persistence:
    enabled: false

whisk:
  ingress:
    apiHostName: {{ API_HOST_NAME }}

controller:
  imageName: "whisk/controller"
  imageTag: "latest"

invoker:
  imageName: "whisk/invoker"
  imageTag: "latest"
  containerFactory:
    impl: kubernetes
    kubernetes:
      agent:
        enabled: false
